#!/usr/bin/env python3

var = -1
if var < 0:
    print(f"{var} the value of var is negative")
#if there is only a single cluse the it may go on the same line as the header statement
if var == -1: print("the value of var is negative")
    